[package]
authors = ["Xavier Lau <xavier@inv.cafe>"]
edition = "2021"
name    = "am-signer"
version = "0.0.0"

[dependencies]
# crates.io
array-bytes = { version = "*" }
clap        = { version = "*", features = ["derive"] }
serde       = { version = "*", features = ["derive"] }
serde_json  = { version = "*" }

# polkadot-sdk
sp-core = { version = "*" }

[workspace]
exclude = []
